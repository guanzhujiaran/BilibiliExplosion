# 根上下文构建时排除无关大目录，减小 build context 体积
puppeteer_Bili/
unidbgSpringBoot/
go-proxy-ipv6-pool-auto/
docker_vol/
dev_init_chmod.bash
pm2.app.js
Makefile
README.md

# Python 缓存 / 虚拟环境 / 本地依赖产物
**/__pycache__/
**/*.pyc

# setuptools 就地构建产物：build/lib 里的旧文件会被 build_py 复用并打进 wheel，
# 导致镜像内装的是过期代码（曾引发 bili_common.models 循环导入）——一律排除。
**/build/
**/*.egg-info/

**/.venv/
**/.pytest_cache/
**/.ruff_cache/
**/node_modules/
**/.git/
