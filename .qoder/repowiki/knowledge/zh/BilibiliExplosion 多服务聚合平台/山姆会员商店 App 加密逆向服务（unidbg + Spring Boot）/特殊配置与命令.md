构建需 JDK 21 并通过 Maven Wrapper (`./mvnw`) 执行；打包镜像名称为 `unidbg-springboot-samsclub`（spring-boot-maven-plugin 配置）。运行时要求 `resources/apk/samsclub/cn.samsclub.app_5.0.120.apk` 与 `resources/so/samsclub/libsrmscy.so` 存在于 classpath，且宿主系统需提供 32 位 ARM 动态链接器以加载 libsrmscy.so（可通过 docker-compose.yml 提供的镜像环境运行）。