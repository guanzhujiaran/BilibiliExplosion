Python 异步脚本，使用 `argparse` 构建 CLI、`langchain_openai.ChatOpenAI` 调用大模型（支持自定义 base_url/token/headers）、`tqdm` 进度条、`aiomysql` (`mysql+aiomysql://`) 直连 MySQL、`loguru` 日志；运行方式统一为 `uv run python -m scripts.judge_grand_prize`。