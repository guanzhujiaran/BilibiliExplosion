通过 `uv run python -m scripts.judge_grand_prize` 执行，支持 `--type all|common|reserve|official`、`--batch-size`、`--concurrency`、`--dry-run`、`--force-update`、`--limit` 以及 `--llm-base-url/--llm-token/--llm-model/--llm-headers`、`--db-host/--db-port/--db-user/--db-password` 等参数；必须确保 `.env` 或命令行中配置了可用的云端 LLM（base_url 与 model_name 均非空），否则脚本直接退出。