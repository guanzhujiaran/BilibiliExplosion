模块以单例模式运行：`get_others_lot_dyn.py` 末尾创建全局实例 `get_others_lot_dyn = GetOthersLotDyn()`，并通过 `get_other_lot_redis_manager` 共享 Redis 连接；测试入口在文件末尾的 `if __name__ == '__main__'` 中用 `asyncio.run` 调用 `_test_supplement` / `_test_cull`。