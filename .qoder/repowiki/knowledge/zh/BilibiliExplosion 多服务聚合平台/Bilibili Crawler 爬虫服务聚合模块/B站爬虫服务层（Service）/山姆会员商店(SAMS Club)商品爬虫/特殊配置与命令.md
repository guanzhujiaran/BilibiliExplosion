运行入口为 `python -m Service.samsclub.main`（或直接 `if __name__ == '__main__'` 调用 `asyncio.run(sams_club_crawler.main())`）；首次运行需在同级目录放置 `auth_token.txt`（登录凭证）与可选的 `app_storage.json`（缓存用户/门店信息），否则 `SamsClubApi.__init__` 会跳过状态恢复。