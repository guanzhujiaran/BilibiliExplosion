运行前需确保 `src/Tools/Enc/node_modules` 已通过 npm 安装（由 `package.json` 管理），以便 execjs 加载 JS 环境；爬虫目标用户列表与最大间隔时间通过编辑 `src/spider/spiderSetting.json` 配置；数据库路径在 `src/ToutiaoSetting.py` 中指向 `/ToutiaoDb/SpaceFeedData.db`。