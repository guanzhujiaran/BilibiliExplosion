Python 3.10+ 的 `match/case` 模式匹配；基于 `Service.BaseCrawler.launcher.scheduler_launcher` 提供的 `GenericCrawlerScheduler` / `BaseScheduler` 进行 cron 调度；告警通过 `Utils.推送.PushMe` 的 `a_pushme` 推送；健康状态基于 `Service.BaseCrawler.plugin.statusPlugin.StatsPlugin` 与 `CrawlerHealthStatus`。