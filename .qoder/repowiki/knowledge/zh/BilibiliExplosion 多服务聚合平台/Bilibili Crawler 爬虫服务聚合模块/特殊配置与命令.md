通过 `python main.py` 或 `uvicorn main:app` 启动 FastAPI 服务；使用 `alembic -c alembic.ini upgrade head` 在各子目录（bili_reserve/biliopusdb/dyndetail/proxy_db/samsclub）下执行对应库的迁移；运维脚本位于 scripts/ 下，如 `python scripts/judge_grand_prize/__main__.py` 执行大奖判定、`python scripts/database/同步向量数据库/sync_bili_lottery_data.py` 同步 Milvus。