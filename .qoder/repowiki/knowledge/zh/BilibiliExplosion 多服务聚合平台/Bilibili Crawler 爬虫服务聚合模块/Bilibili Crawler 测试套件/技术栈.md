pytest + pytest-asyncio；HTTP 测试使用 httpx AsyncClient + ASGITransport；MQ 集成测试使用 FastStream TestRabbitBroker；数据库测试使用 SQLAlchemy async engine + sqlite+aiosqlite 内存库；通过 unittest.mock / monkeypatch 隔离外部 RabbitMQ、Redis、MySQL、LLM、UNIDBG、V2Ray 等依赖。