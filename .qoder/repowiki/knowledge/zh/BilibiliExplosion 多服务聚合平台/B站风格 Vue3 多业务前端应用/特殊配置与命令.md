构建与开发命令由 Vite 驱动（见 package.json），环境变量通过 `.env.development`、`.env.prod`、`.env.prod.example` 注入（如 `VITE_GRAPH_API`、`VITE_CASDOOR_*`、`VITE_CLARITY_ID`）；hey-api 客户端通过 `codegen.ts` 从后端 OpenAPI/GraphQL schema 生成。