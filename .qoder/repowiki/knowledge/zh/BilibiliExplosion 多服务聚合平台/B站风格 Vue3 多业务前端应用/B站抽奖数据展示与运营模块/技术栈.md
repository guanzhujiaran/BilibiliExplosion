Vue3 `<script setup>` + TypeScript；Element Plus 作为 UI 基础组件库；hey-api 生成的 `V1BiliService` 客户端访问后端 OpenAPI 服务；Tailwind 工具类用于样式；使用 `el-drawer`、`el-pagination`、`el-countdown`、`el-collapse` 等 Element Plus 组件构建交互。