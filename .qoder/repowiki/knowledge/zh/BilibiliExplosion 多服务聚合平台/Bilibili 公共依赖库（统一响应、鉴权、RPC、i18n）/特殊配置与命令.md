使用 uv 管理依赖并配置阿里云镜像源（`pyproject.toml`），通过 `uv sync` 安装开发依赖；通过 `babel.cfg` 扫描 `bili_common/**.py` 提取 `_`/`gettext`/`ngettext` 标记的字符串，由 `fill_po.py` 填充翻译模板，`verify_i18n.py` 校验翻译完整性；Docker 构建时设置 `UV_NO_EDITABLE=1` 强制非 editable 安装以便复制进 site-packages。