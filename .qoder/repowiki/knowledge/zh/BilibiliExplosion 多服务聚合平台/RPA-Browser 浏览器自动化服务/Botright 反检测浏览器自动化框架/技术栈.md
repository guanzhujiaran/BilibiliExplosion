Python 异步框架（`asyncio` + `async_class.AsyncObject`），底层基于 `playwright.async_api` 与可选的 `patchright.async_api`（undetected-playwright）；指纹伪造使用 `browserforge.fingerprints`；代理探测使用 `httpx` 异步客户端；Chrome 内核发现使用 `browsers` 包。