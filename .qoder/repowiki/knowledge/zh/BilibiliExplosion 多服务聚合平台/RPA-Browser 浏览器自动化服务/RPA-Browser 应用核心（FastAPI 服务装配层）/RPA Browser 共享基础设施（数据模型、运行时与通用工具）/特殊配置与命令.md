启动时需先执行 `alembic_migration.run_alembic_upgrade_head()` 完成增量迁移，再调用 `check_schemas()` 校验模型与数据库 schema 一致，若存在 critical 差异会拒绝启动并提示执行 `alembic upgrade head`；数据库连接通过 `settings.mysql_browser_info_url` 配置，MySQL 场景需确保 `wait_timeout` > 300s 以匹配 `pool_recycle=300`。