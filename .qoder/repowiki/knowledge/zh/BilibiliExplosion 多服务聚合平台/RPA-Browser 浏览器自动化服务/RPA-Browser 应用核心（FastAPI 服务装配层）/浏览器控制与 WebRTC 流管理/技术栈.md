FastAPI 路由 + asyncio 协程；Playwright 浏览器池（`PlaywrightSessionPool`）；WebRTC 流通过 `WebRTCStreamManager` 管理，使用 `weakref` 打破循环引用，`collections.OrderedDict` 实现 LRU 淘汰；日志使用 loguru，调度任务通过 `scheduler_manager_ist` 注入。