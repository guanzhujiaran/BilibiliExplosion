- Action 类测试统一通过 `page` fixture 获取共享页面，并使用 `@pytest.mark.asyncio(loop_scope="session")` 标记异步测试方法。
- 控制器路由测试统一使用 `httpx.AsyncClient(transport=ASGITransport(app))` 挂载 FastAPI，并通过 `dependency_overrides[get_auth_info_from_header]` 注入 mock 认证信息。
- 测试数据通过 conftest 中的 `created_action` / `created_workflow` / `created_plugin` fixture 创建，返回 `(db_id, id)` 元组供用例复用。
- 数据库清理采用 autouse fixture，按外键依赖倒序删除模型以避免约束冲突。
- 断言统一检查响应体中的 `code` 字段并与 `ResponseCode.SUCCESS` / `NOT_FOUND` 等常量比较，而非直接断言 status_code。