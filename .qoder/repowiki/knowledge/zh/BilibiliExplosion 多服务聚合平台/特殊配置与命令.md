使用 `make all` 或 `make start` 启动全部服务，`make start-excluding-fastapi` 跳过 Python 服务仅拉起基础设施与 Node.js 服务；`make logs [SERVICE]` 查看指定容器日志；首次克隆需执行 `make submodule-init` 初始化 git submodule，`make update` 同步远端最新子模块。