通过 `package.json` scripts 启动：`npm run dev` / `npm run prod` 分别以 dev/prod 环境运行 `ExpressServerEnd/ServerRun.js`；数据库迁移使用 `npm run migrate` / `migrate:undo` / `migrate:status`；模型生成使用 `npm run migration:gen`，基线设置使用 `npm run migrate:baseline`；独立自动化调度进程直接执行 `node index.js`，需配合 `.env` 配置。