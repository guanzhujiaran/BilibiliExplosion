启动时需确保 `app/.env` 存在以加载 `.env` 文件；首次运行会调用 `ensure_database()` 自动创建 MySQL 主库，并执行 `alembic upgrade head` 拉齐主库与 pptr 库 Schema；私信内容分表由 `ensure_current_month_shards()` 预热当月 100 张表，历史月份按需懒创建；GeoIP mmdb 需挂载至 `./mmdb` 目录。