pydantic-settings 管理配置；SQLAlchemy async engine + SQLModel 会话（MySQL 隔离级别 READ COMMITTED，pptr Postgres 独立引擎）；FastStream RabbitMQ 通过 TOPIC exchange `message_exchange` 路由；Alembic 双分支迁移（alembic/ 与 alembic_pptr/）；自研 SnowflakeIdGenerator / MinuteSnowflakeIdGenerator 生成 msgkey、uid、moment_id、topic_id。