SQLModel + SQLAlchemy（asyncio session）直连 pptr Postgres；Casdoor Python SDK (`AsyncCasdoorSDK`) 处理 OAuth2；MySQL `INSERT ... ON DUPLICATE KEY UPDATE` 实现幂等关注/拉黑；loguru 日志；httpx 直接调用 Casdoor REST 接口作为 SDK 回退路径。