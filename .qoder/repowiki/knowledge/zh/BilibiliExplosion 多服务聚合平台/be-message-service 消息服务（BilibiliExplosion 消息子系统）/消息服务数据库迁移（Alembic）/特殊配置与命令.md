主库迁移通过 `alembic -c alembic.ini ...` 运行，连接 `settings.mysql_message_url`；pptr 迁移通过 `alembic -c alembic_pptr/alembic.ini ...` 运行，自动将 `postgresql+asyncpg://` 替换为 `postgresql+psycopg2://`。生成全量 base migration 时可临时切换 `_USE_TEMP_DB_FOR_AUTOGEN` 到 `BiliMessageDB_temp`。