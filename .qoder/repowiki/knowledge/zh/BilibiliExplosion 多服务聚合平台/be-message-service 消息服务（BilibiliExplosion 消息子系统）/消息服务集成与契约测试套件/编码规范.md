- 每个测试文件通过 `@pytest.fixture(autouse=True)` 的 `_bind_engine_per_test` 在每个测试的事件循环内重建 async engine 并替换 `app.core.database` 模块级单例，避免跨测试事件循环冲突。
- 测试数据使用固定高位 mid / oid 区间（如 `901_000+`、`882_000_000_000+`）并通过 `_cleanup` 函数在 `try/finally` 中按表顺序 DELETE 回滚，保证用例幂等与互不干扰。
- 对需要真实宿主行的场景（如 DYNAMIC 评论计数回写），通过 `_ensure_moment` 或 `_seed_dynamic` 在 `TMoment`、`TInteractionStat`、`TResourceFeed` 中插入最小化种子数据后再执行业务逻辑。
- 对于审计前置流程，测试先调用 `CommentService.add` 得到 auditing 状态，再调用 `_pass_audit` 将状态置为 NORMAL 并手动补齐 `root_count/all_count`，以绕过实际审核链路。
- Moment 相关测试通过 `seed_biliopus.fetch_real_dyns` 拉取真实动态正文填充 `contentText`/`contentJson`，而非手工构造假文本，使测试数据贴近线上形态。
- 对 RabbitMQ、RPC、第三方推送等外部依赖，测试采用直接调用 Service 层方法 + 打桩 `publisher.publish_dm_content` 的方式，仅验证主库索引与核心逻辑而不启动消息中间件。