运行需配置 `app/.env` 中的 `mysql_message_url`（消息库）和可选 `postgres_pptr_url`；若启用 Moment 系列测试，还需在同一 MySQL 实例中存在 `biliopusdb` 库且包含 `t_lotdyninfo`、`t_lot_extra_info` 表。单个测试可执行示例：`uv run pytest tests/test_push_rpc.py -v`。