GeoIP 功能需在运行环境放置 GeoLite2-City.mmdb、GeoLite2-ASN.mmdb（默认路径 `settings.geoip_mmdb_dir`，Docker 通过 volume 挂载，本地可用 `scripts/download_geoip_mmdb.py` 下载）；JWT 算法与密钥需与 pptr 侧 `common_config.salt.jwt_secret` 保持一致。